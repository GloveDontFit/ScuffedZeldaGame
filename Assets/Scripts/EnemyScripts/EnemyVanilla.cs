using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class EnemyVanilla : MonoBehaviour
{
    
    public EnemyState currentState;
    public float health;
    public FloatValue maxHealht;
    public int moveSpeed;
    public int baseAttack;
    public string enemyName;
    private void Start()
    {
        health = maxHealht.initialValue;
    }
    public void knock(Rigidbody2D target,float knockTime)
    {
        StartCoroutine(Knockback(target, knockTime));
    }
    IEnumerator Knockback(Rigidbody2D target,float knockbackTime)
    {


        if (target!=null)
        {
            yield return new WaitForSeconds(knockbackTime);
            target.velocity = Vector2.zero;

            
            currentState = EnemyState.idle;
            target.velocity = Vector2.zero;

        }
       


    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
*/